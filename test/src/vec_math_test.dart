// ignore_for_file: prefer_const_constructors
import 'package:test/test.dart';
import 'package:vec_math/vec_math.dart';

void main() {
  group('VecMath', () {
    test('can be instantiated', () {
      expect(VecMath(), isNotNull);
    });
  });
}
